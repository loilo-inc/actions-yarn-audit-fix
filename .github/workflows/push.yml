name: CI
on: [push]
jobs:
  build:
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: "14"
      - run: tsc --noEmit
      - name: Check lib
        run: |
          make build
          git diff --exit-code