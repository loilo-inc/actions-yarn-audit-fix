name: CI
on: [push]
jobs:
  build:
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: "14"
      - name: Check lib
      - run: tsc --noEmit
        run: |
          make build
          git diff --exit-code